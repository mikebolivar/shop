<%= form_for(@webhook) do |f| %>
  <% if @webhook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webhook.errors.count, "error") %> prohibited this webhook from being saved:</h2>

      <ul>
      <% @webhook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shopify_webhook_id %><br>
    <%= f.number_field :shopify_webhook_id %>
  </div>
  <div class="field">
    <%= f.label :event %><br>
    <%= f.text_field :event %>
  </div>
  <div class="field">
    <%= f.label :object_json %><br>
    <%= f.text_area :object_json %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
